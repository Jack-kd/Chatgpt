# 网页转软件一键生成器

这是一个前端离线小工具：输入网址、包名和图标后，可一键生成 Android Studio 工程 ZIP。

## 使用方式

1. 直接打开 `index.html`，或用任意静态服务器启动。
2. 填入：应用名称、网址、包名，选择图标。
3. 点击“一键生成 Android 应用工程 ZIP”。
4. 解压后用 Android Studio 打开并构建 APK。

## 生成后的应用特性

- WebView 顶部加载进度条。
- 兼容系统 VPN/代理网络栈。
- 自动保存上次浏览页面并恢复。
- 下拉刷新、后退返回、基础异常提示。
